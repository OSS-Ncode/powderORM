generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
  moduleFormat = "esm"
}

datasource db {
  provider = "sqlite"
}

model BenchUser {
  id    Int    @id
  name  String
  score Float?

  @@map("bench_users")
}
